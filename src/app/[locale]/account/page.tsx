import { notFound } from 'next/navigation'; import { InternationalAccountPage } from '@/components/auth/international-account-page'; import { isInternationalLocale } from '@/lib/i18n';
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; return { title: locale === 'en' ? 'My account' : 'Mi cuenta', robots: { index: false, follow: false } }; }
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isInternationalLocale(locale)) notFound(); return <><script dangerouslySetInnerHTML={{ __html: `document.documentElement.lang=${JSON.stringify(locale)};` }} /><InternationalAccountPage locale={locale} /></>; }
