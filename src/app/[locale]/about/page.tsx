import { InternationalPublicPage } from '@/components/marketing/international-public-page'; import { isInternationalLocale } from '@/lib/i18n'; import { notFound } from 'next/navigation';
import { internationalSeo } from '@/lib/i18n-seo';
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isInternationalLocale(locale)) return {}; return { title: locale === 'en' ? 'About Resolva Jato' : 'Acerca de Resolva Jato', description: locale === 'en' ? 'Learn about Resolva Jato and the team operating the platform.' : 'Conoce Resolva Jato y el equipo que opera la plataforma.', ...internationalSeo(locale, 'about', '/sobre') }; }
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isInternationalLocale(locale)) notFound(); return <><script dangerouslySetInnerHTML={{ __html: `document.documentElement.lang=${JSON.stringify(locale)};` }} /><InternationalPublicPage locale={locale} page="about" /></>; }
