import { Suspense } from 'react'; import { notFound } from 'next/navigation'; import { InternationalVerifyEmail } from '@/components/auth/international-verify-email'; import { isInternationalLocale } from '@/lib/i18n';
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; return { title: locale === 'en' ? 'Email confirmation' : 'Confirmación de correo', robots: { index: false, follow: false } }; }
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isInternationalLocale(locale)) notFound(); return <><script dangerouslySetInnerHTML={{ __html: `document.documentElement.lang=${JSON.stringify(locale)};` }} /><Suspense><InternationalVerifyEmail locale={locale} /></Suspense></>; }
