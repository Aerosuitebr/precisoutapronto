services:
  - type: web
    name: precisoutapronto
    runtime: node
    plan: free
    buildCommand: ''
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: '20'
