---
description: Identidade, stack e mapa do Precisou, Tá Pronto (hub-recursos-gratis)
alwaysApply: true
---

# Precisou, Tá Pronto · contexto do projeto

Você está no monorepo/app **Precisou, Tá Pronto** (pasta local `hub-recursos-gratis`).

| Item | Valor |
|------|--------|
| Produto | Precisou, Tá Pronto · ferramentas gratuitas (orçamento, currículo, recibo, rescisão, etc.) |
| Repo GitHub | `Aerosuitebr/precisoutapronto` |
| Branch padrão | `master` |
| Pasta local | `D:\Desenvolvimento\hub-recursos-gratis` |
| Package | `precisoutapronto-precisoutapronto` |
| Stack | Next.js 15 · React 18 · Prisma 5 · PostgreSQL · Docker |
| Auth/pagamentos | sessão própria · Stripe · Mercado Pago · WhatsApp via Evolution |
| Copy | regra `copy-sem-travessao.mdc` (sem `—` / `–`) |

## Ambientes

| Ambiente | URL | Código no VPS | Porta host | Compose |
|----------|-----|---------------|------------|---------|
| Produção | https://precisoutapronto.com.br | `/opt/precisoutapronto` | `127.0.0.1:3000` | `docker-compose.yml` + `docker-compose.vultr.yml` |
| Staging | https://staging.precisoutapronto.com.br | `/opt/precisoutapronto-staging` | `127.0.0.1:3001` | `docker-compose.staging.yml` · projeto `precisoutapronto-staging` |

- VPS Vultr (mesmo host do Aerosuite): `216.238.102.195`, SSH `root` + chave `~/.ssh/aerosuite_ed25519` (local) / secret `VULTR_*` (CI).
- Exposição pública: Cloudflare Tunnel (`cloudflared-precisoutapronto`), **sem Caddy** no Vultr.
- Staging: `APP_ENV=staging`, noindex, Cloudflare Access (`@aerosuite.com.br`).

## Mapa útil do código

- App / rotas: `src/app/`
- Lib / domínio: `src/lib/`
- Crescimento (nichos): `src/lib/growth/segments.ts`, `src/lib/growth/intents.ts`
- Schema DB: `prisma/schema.prisma`
- Migrations: `prisma/migrations/`
- Deploy: `scripts/deploy/`, `.github/workflows/deploy-vultr.yml`
- Docs operacionais: `DOCKER.md`, `docs/E2E.md`, `docs/ARQUITETURA-CRESCIMENTO.md`, `docs/CLOUDFLARE_TUNNEL_PRECISOUTAPRONTO.md`
- `tmp/` é lixo local: **nunca** commitar.

## Comportamento do agente

1. Ao iniciar tarefa de deploy, banco, SEO ou infra: leia esta regra e os docs citados; não redescubra o projeto do zero.
2. Nunca invente secrets. Credenciais ficam em `.env` / `.env.production` / `.env.staging` no servidor (não versionados).
3. Prefira staging antes de produção quando houver migration ou mudança de schema.
4. Responda em português.
