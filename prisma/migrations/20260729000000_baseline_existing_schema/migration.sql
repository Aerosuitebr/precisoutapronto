-- Baseline for databases that already existed before Prisma Migrate
-- (created previously via prisma db push). This file intentionally has
-- no DDL. On first adopt, the entrypoint marks it applied, then runs
-- subsequent additive migrations with prisma migrate deploy.

SELECT 1;
